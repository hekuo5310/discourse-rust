{
  "name": "forum-engine-worker",
  "private": true,
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply forum-engine-db --local",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "4.122.0"
  }
}

